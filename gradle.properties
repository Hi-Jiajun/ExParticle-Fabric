# 内存 / 并行
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# 与 nbaurora / nbmachina 同轴：MC 1.21.10 Fabric
minecraft_version=1.21.10
loader_version=0.19.5
loom_version=1.17.20
fabric_api_version=0.138.4+1.21.10

# Mod 属性（上游 v1.5.2 的 Fabric 移植）
mod_version=1.5.2-fabric.3
maven_group=net.hackermdch
archives_base_name=exparticle-fabric

# 需要 HTTP 代理时在命令行传，别写进本文件（避免把个人网络配置带进仓库）：
#   .\gradlew.bat -Dhttp.proxyHost=<host> -Dhttp.proxyPort=<port> build
